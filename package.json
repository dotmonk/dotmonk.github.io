{
  "name": "dotmonk.github.io",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "npm install && rm -rf docs && snowpack build && touch docs/.nojekyll node_modules/.nojekyll",
    "dev": "npm install && snowpack dev"
  },
  "devDependencies": {
    "@types/react": "^18.0.35",
    "@types/react-dom": "^18.0.11",
    "snowpack": "^3.8.8"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "css-nyx-theme": "https://github.com/dotmonk/css-nyx-theme.git#master",
    "react-nyx-components": "https://github.com/dotmonk/react-nyx-components.git#master"
  }
}
